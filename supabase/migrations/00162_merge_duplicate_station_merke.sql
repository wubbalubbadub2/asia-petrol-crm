-- 00162_merge_duplicate_station_merke.sql — ОТМЕНЕНА, НЕ ПРИМЕНЯЛАСЬ.
--
-- Файл оставлен пустым намеренно: номер 00162 уже был занят в рабочей
-- копии, и переиспользовать его под другую правку нельзя.
--
-- Что здесь было. Слияние двух записей «ст. Мерке» (departure +
-- destination) в одну с типом 'both'. Клиент 2026-09-19 отказался:
-- «Если есть тип станции, то нужно его оставить и пусть будут две
-- станции с одинаковым именем, но с разными типами. Нам лучше логику
-- починить». То есть справочник остаётся как есть, а чинить нужно
-- ссылки, которые указывают на запись не той роли, и сам выбор в
-- интерфейсе.
--
-- Замена: 00163_fix_station_type_references.sql — переставляет ссылки
-- на одноимённую станцию нужного типа, ничего не сливая и не удаляя.
--
-- На боевой базе эта миграция не выполнялась.
